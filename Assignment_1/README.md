                                   ******************main.py******************
This python program perform arithmetic operation like addition subtraction
multiplication and divide
********************************************************************************************
-> Take two num1, num2 input by user and convert in float formate because when you take input by user by 
default that value perform like string


num1 = float(input("Enter the first number: "))
num2 = float(input("Enter the second number: "))
********************************************************************************************
-> Now perform arithmetic operation like like addition subtraction
multiplication and divide

addition = num1 + num2
subtraction = num1 - num2
multiplication = num1 * num2
if num2 != 0:
    division = num1 / num2
else:
    division = "Undefined (cannot divide by zero)"
********************************************************************************************
-> After performing all operation print all value

                                   ****************greeting.py****************
-> This python program perform print greeting
-> Take two value fanme, lname input by user, this value by default string so no need to convert
fname = input("Enter the first Name: ")
lname=  input("Enter the Last Name : ")
Now concate and print above input

